<%= form_tag :controller => 'searches', :action => 'search' do %>
      <%= text_field_tag :search, params[:search], :id => 'search_field', :class =>"form-control" %>
      <%= hidden_field_tag :model, params[:model], :id => 'model', :value => 'user'  %>
        <button name='nil' type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
      <!--<%= link_to "limpar", "$('search_field').clear()" %>-->
<% end %>