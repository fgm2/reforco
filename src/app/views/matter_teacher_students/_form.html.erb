<%= form_for(@matter_teacher_student) do |f| %>
  <% if @matter_teacher_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matter_teacher_student.errors.count, "error") %> prohibited this matter_teacher_student from being saved:</h2>

      <ul>
      <% @matter_teacher_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :matter_id %><br>
    <%= f.number_field :matter_id %>
  </div>
  <div class="field">
    <%= f.label :teacher_id %><br>
    <%= f.number_field :teacher_id %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :many %><br>
    <%= f.number_field :many %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
